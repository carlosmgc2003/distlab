{
  "name": "distlab",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w @distlab/contracts",
    "typecheck": "npm run typecheck -w @distlab/contracts",
    "test": "npm test -w @distlab/contracts"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
